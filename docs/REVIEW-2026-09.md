# Portal review — 22 Sep 2026

## Done
| Area | Change |
|---|---|
| Security | Self-signup blocked on Revive Apps (`private.block_unlisted_signup` trigger + `private.signup_allow`). Was the top open July finding: any stranger could register and read CVs, CRM, recipes. |
| Security | `cafe-today` (daily sales, ad spend, CPA) now login-only, no `CORS *`. Background workers (Xero sync, naughty scan, POS report, ads video/sync, catering, enrich) no longer triggerable by anyone — internal header or run key. Closed the `next_run` bypass on the ads workers. |
| Security | RLS on the 15 tables that lacked it; search_path pinned on 5 functions; `esc()` escapes quotes on Portal, Music, Pulse. |
| Consistency | One guard `_runkey.js` for every non-browser call. Revived 8 Claude tools that had been rejecting everything (`ads-audit`, `meta-config`, `tk-daily`, `shopify-probe`, `*-run`). |
| Cleanup | 13 dead functions removed (−1,650 lines); 11 unused tables parked in `zz_archive` (drop after Dec 2026 if not missed); unused ffmpeg scoping removed. |
| Speed | Index for the till agent's queue poll (was scanning 12k rows every few seconds). |
| Docs | `docs/STACK.md`, one doc per app, `_TEMPLATE.md`, Guide tile. |

## Per-app verdict
| App | Code health | Pattern | Biggest issue |
|---|---|---|---|
| Scorecard, Tasks, Checklist, Recon, Ads, Timeclock, Training | Clean | Function-only ✅ | Timeclock/Training not in use yet |
| Support | Good, sprawling (45 functions) | Function-only ✅ | 5-min sync rewrites everything |
| Production | Good | Mixed | Browser writes runs directly |
| Sales | Heavy (170 KB, 100 commits) | Legacy | Stale-deploy regressions ×2 |
| Recipes, Pulse | OK | Legacy | Unused Meals feature (Recipes) |
| Jobs | OK, largest page | Exception | Two repos per change |
| Rostering | Dense vanilla JS, 7 dead functions | Portal DB direct | No HTML escaping |

## Recommended — easy wins, mostly simplification
| # | Win | Effort | Why |
|---|---|---|---|
| 1 | Hide Training and Timeclock tiles until they go live (or load 5 SOPs / set a go-live date) | 5 min | Empty apps add clutter and upkeep |
| 2 | Cut never-used sub-features: Recipes→Meals, Scorecard issues/actions/evidence, Checklist photos/handover, Tasks check-ins | 1 hr | Less code to keep consistent; Scorecard brief was "super simple" |
| 3 | Retention: delete `pos_paste` / `pos_jobs` rows older than 60 days nightly | 15 min | 25k rows of logs growing forever |
| 4 | ✅ Done 22 Sep — Support sync change-detection (skip unchanged threads) | — | Removes most DB writes, Gmail and Shopify calls |
| 5 | Move Recipes, Sales, Pulse, Production writes to one `<app>-data.js` each | 1 day each | Deletes 4 session functions, 3 shared logins, 3 password env vars; one pattern everywhere |
| 6 | Precompile one shared Tailwind CSS file instead of the Play CDN on 13 pages | 2 hr | Faster first load on every app (Production already felt slow) |
| 7 | Deploy guard against stale overwrites: agents diff against `origin/main` and never paste whole files | rule, done in AGENTS.md | Source of most "restore" commits |
| 8 | Music mp3s → Supabase Storage | 1 hr | ~75 MB in git slows every deploy clone |
| 9 | Fold Support's 45 single-purpose functions into ~5 action functions over time | ongoing | Same features, one-tenth the files |

## Needs Jeremy (settings Claude can't change)
1. Supabase → both projects → Authentication: turn on leaked-password protection; minimum password 10.
2. GitHub → repo Settings → make private (nothing breaks).
3. GitHub → replace the deploy PAT with a fine-grained token scoped to this repo (contents: write).
4. At the till PC: re-run the POS agent install with a new key (then set `POS_AGENT_KEY`).
5. Supabase → Revive Apps → Auth: disable signups (the trigger already blocks them; this is belt-and-braces).
