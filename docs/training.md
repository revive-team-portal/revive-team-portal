# Training — `/training/`

**Purpose:** Staff training library: docs with versions and steps, quizzes, sessions, requirement matrix; staff complete via `/me/`.

**Who uses it:** Admins author; staff via `/me/`.

## Key features
- Library, editor, sessions, matrix, people, feedback, export; AI drafting (`training-ai`)

## Data
Schema `training` (15 tables) — **all currently empty**.

## Functions
`training-admin`, `training-ai`, `me-data`.

## Programming points (keep future changes consistent)
- Current standard pattern.

## Open items / review notes (Sep 2026)
- No content yet. Either load the first 5 SOPs or hide the tile until ready.
